I will strictly follow the "Basic Picture Editing" section of the provided documentation.

1.  **Register VueCropper Plugin**
    *   Modify `src/main.ts`:
        *   Import `VueCropper` and `vue-cropper/dist/index.css`.
        *   Register it using `app.use(VueCropper)`.

2.  **Create ImageCropper Component**
    *   Create `src/components/ImageCropper.vue`.
    *   **Template**: Use `a-modal` wrapping `vue-cropper` and action buttons (Rotate Left/Right, Zoom In/Out, Confirm).
    *   **Script**:
        *   Define props: `imageUrl`, `picture`, `spaceId`, `onSuccess`.
        *   Implement `openModal` and `closeModal`.
        *   Implement `rotateLeft`, `rotateRight`, `changeScale`.
        *   Implement `handleConfirm`: Get crop blob, create file, and call `uploadPictureUsingPost`.
    *   **Style**: Add necessary CSS for the cropper container.

3.  **Integrate into AddPicturePage**
    *   Modify `src/pages/AddPicturePage.vue`.
    *   Import `ImageCropper` component and `EditOutlined` icon.
    *   Add the "Edit Picture" button and `ImageCropper` component to the template.
    *   Implement `doEditPicture` to open the modal.
    *   Implement `onCropSuccess` to update the picture data.

I will not implement the Proxy configuration blindly as it requires the specific Object Storage domain, which varies per project. I will assume the CORS is handled on the server side or I will wait for user feedback if they need the proxy setup.